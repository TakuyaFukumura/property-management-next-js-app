# 実装進捗調査（設計資料との差分）

## 1. 調査目的

`docs` 配下の設計資料（機能要件・画面要件・API要件・詳細設計）と現状実装を照合し、実装済み範囲と未実装範囲を整理する。

## 2. 調査対象

- 設計資料
  - `/tmp/workspace/TakuyaFukumura/property-management-next-js-app/docs/05_機能要件定義.md`
  - `/tmp/workspace/TakuyaFukumura/property-management-next-js-app/docs/09_画面要件定義.md`
  - `/tmp/workspace/TakuyaFukumura/property-management-next-js-app/docs/10_API要件定義.md`
  - `/tmp/workspace/TakuyaFukumura/property-management-next-js-app/docs/11_詳細設計.md`
- 実装コード
  - `/tmp/workspace/TakuyaFukumura/property-management-next-js-app/src/app`
  - `/tmp/workspace/TakuyaFukumura/property-management-next-js-app/lib/property-management.ts`
  - `/tmp/workspace/TakuyaFukumura/property-management-next-js-app/src/app/api/message/route.ts`

## 3. 全体サマリー

- 現状は **「サンプルデータを表示するMVPモックUI + 集計ロジック」中心** の実装。
- 主要な一覧/可視化画面（ダッシュボード、物件一覧、部屋一覧、月次収支、修繕履歴、要対応一覧）は表示可能。
- 一方で、設計書で定義されている **登録・編集・検索/絞り込み・API群・永続化連携** は未実装。

## 4. 機能要件（docs/05）ベース進捗

| 機能ID | 機能名 | 進捗 | 根拠（主な実装箇所） | 残実装 |
|---|---|---|---|---|
| PM-PROP-001 | 物件一覧表示 | 実装済み | `/src/app/properties/page.tsx` | 検索・並び替え |
| PM-PROP-002 | 物件登録 | 未実装 | 登録フォーム/APIなし | 登録UI・バリデーション・保存処理 |
| PM-PROP-003 | 物件編集 | 未実装 | 編集UI/APIなし | 編集UI・更新処理 |
| PM-ROOM-001 | 部屋一覧表示 | 実装済み | `/src/app/units/page.tsx` | ステータス絞り込み |
| PM-ROOM-002 | 部屋登録 | 未実装 | 登録フォーム/APIなし | 登録UI・保存処理 |
| PM-ROOM-003 | 部屋編集 | 未実装 | 編集UI/APIなし | 編集UI・更新処理 |
| PM-INC-001 | 収入登録 | 未実装 | 登録フォーム/APIなし | 登録UI・保存・集計再計算 |
| PM-EXP-001 | 支出登録 | 未実装 | 登録フォーム/APIなし | 登録UI・保存・集計再計算 |
| PM-SUM-001 | 月次収支サマリー表示 | 一部実装 | `/src/app/finance/page.tsx` | 月切替・物件絞り込み |
| PM-REP-001 | 修繕履歴登録 | 未実装 | 登録フォーム/APIなし | 登録UI・保存処理 |
| PM-REP-002 | 修繕履歴一覧表示 | 実装済み | `/src/app/repairs/page.tsx` | 絞り込み |
| PM-TASK-001 | 要対応一覧表示 | 一部実装 | `/src/app/tasks/page.tsx`, `buildDashboardSnapshot` | 種別/優先度絞り込み、詳細遷移 |
| PM-DASH-001 | ダッシュボード表示 | 一部実装 | `/src/app/page.tsx`, `buildDashboardSnapshot` | 対象月切替 |
| PM-CSV-001 | CSVエクスポート | 未実装 | CSV API/出力処理なし | CSV出力API・ダウンロード導線 |

### 進捗感（機能要件ベース）

- Must（13機能）: 実装済み 3 / 一部実装 3 / 未実装 7
- 全体としては **「可視化系は先行、入力・更新系は未着手」** の状態。

## 5. 画面要件（docs/09）ベース進捗

- 実在画面: `/`, `/properties`, `/properties/[id]`, `/units`, `/finance`, `/repairs`, `/tasks`
- 未実装画面:
  - 部屋登録・編集（SCR-ROOM-002）
  - 収入登録（SCR-INC-001）
  - 支出登録（SCR-EXP-001）
  - 設定画面（SCR-SET-001）
- 既存画面でも、検索/絞り込み/月切替などの操作系要件は未実装。

## 6. API要件（docs/10）ベース進捗

- 設計上は15本定義されているが、現状のAPIルート実装は `GET /api/message` のみ。
- 未実装:
  - ダッシュボード、物件、部屋、収支、修繕、要対応、CSV の各API
- そのため現在のUIは `samplePortfolio` に依存しており、設計書のAPI/DB責務分離（docs/11）には未到達。

## 7. 直近の実装残タスク（優先順）

1. **API基盤整備**: `/api/dashboard`, `/api/properties`, `/api/units`, `/api/monthly-summaries`, `/api/repairs`, `/api/tasks/attention`
2. **登録/編集UI実装**: 物件・部屋・収入・支出・修繕のフォーム
3. **入力バリデーション実装**: docs/11 の定義に沿ったフロント/API両側バリデーション
4. **一覧操作強化**: 検索、絞り込み、並び替え、対象月切替
5. **CSV出力対応**: `/api/export/csv` と画面導線

## 8. 補足（テスト観点）

- 既存テストは主に `buildDashboardSnapshot` の集計・要対応抽出と、主要UIの表示確認（`__tests__/lib/property-management.test.ts`, `__tests__/src/app/page.test.tsx`）。
- 登録/編集/API連携のテストは未整備のため、実装着手時に docs/12 の方針に沿って追加が必要。
